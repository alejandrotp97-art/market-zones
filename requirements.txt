# Instalación:  pip install -r requirements.txt
#
# Reproducir EXACTAMENTE lo que corre en producción:
#     pip install -r requirements.txt -c constraints.txt
#
# Los `>=` de aquí abajo son suelos: dicen desde qué versión existe la API que
# usa el código. NO son un certificado de que esa versión concreta funcione —
# nadie ha ejecutado nunca la suite contra numpy 1.24 ni pandas 2.0. Lo que sí
# está comprobado son dos cosas, y sólo dos:
#
#   - el juego exacto de `constraints.txt`, que es lo que corre en el servidor
#     (hoy numpy 2.4.4 y pandas 3.0.3, muy por encima de estos suelos);
#   - lo ÚLTIMO de cada paquete, en Python 3.10 y 3.12, que es lo que instala
#     el CI en cada push y una vez por semana.
#
# Es justo el hueco entre esas dos cosas donde vive un `pip install` de alguien
# que sigue la GUIA hoy, y por eso el CI instala este fichero tal cual en vez de
# un juego fijado.
#
# Motor de puntuación (matemática pura, sin red):
numpy>=1.24
pandas>=2.0

# El panel web. El motor no depende de Flask: se puede usar desde la línea de
# comandos (cli.py) sin levantar ningún servidor.
flask>=3.0

# Servidor de producción. Sin él, dashboard.py cae al servidor de desarrollo de
# Flask, que abre un hilo por conexión sin límite: aceptable en local, no cuando
# hay un proxy delante reenviando peticiones de desconocidos.
waitress>=3.0

# Usado por research/ (validación estadística), no por el panel.
scipy>=1.10

# Sólo para los informes en PNG de analysis/. El panel no lo importa.
matplotlib>=3.7

# El adaptador de datos usa únicamente la librería estándar (urllib), así que no
# hacen falta `requests` ni `yfinance`. La normal acumulada sale de math.erf.

# Desarrollo
pytest>=8.0

# Linter. Va en requirements y no sólo en el CI para que se pueda pasar en
# local antes de empujar, que es donde sale barato arreglar lo que encuentra.
ruff>=0.16
